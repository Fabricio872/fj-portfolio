{% extends 'layout.html.twig' %}

{% block body %}
    {% include 'navbar.html.twig' %}
    {% if app.session.flashbag.get('spiderman-easteregg') %}
        <div id="spiderman-easteregg">
            <img src="{{ path('image.asset', {'imageName':'spiderman_easteregg.png', 'size':'medium'}) }}"
                 alt="spidermans pointing at each other">
        </div>
    {% endif %}
    <div class="app" data-bs-spy="scroll" data-bs-target="#navbar-scrollspy" data-bs-smooth-scroll="true" tabindex="0">
        {% include 'app/header.html.twig' %}
        {% include 'app/web-projects.html.twig' %}
        {% include 'app/printed-projects.html.twig' %}
        {% include 'app/github-projects.html.twig' %}
        {% include 'footer.html.twig' %}
    </div>
{% endblock %}
